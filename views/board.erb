<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="utf-8">
        <title><%= @title %></title>
    </head>
    <body>
        <h1>User: <%= @username %></h1>
        
        <form method="post" action="board">
            <table>
                <tr>
                    <th>message</th>
                </tr>
                <tr>
                    <td><input type="text" name="message"></td>
                    <td><input type="submit" value="Post"></td>
                </tr>
            </table>
        </form>

        <table style="border: 2px solid #ccc; width: 100%; border-collapse: collapse; margin-top: 20px;">
            <tr style="border-bottom: 2px;">
                <th style="border: 2px solid #ccc; padding: 8px;">user</th>
                <th style="border: 2px solid #ccc; padding: 8px;">message</th>
                <th style="border: 2px solid #ccc; padding: 8px;">date</th>
            </tr>
            <% @messages.each do |message| %>
                <tr style="border-bottom: 1px dotted #ccc;">
                    <td style="border-right: 1px dotted #ccc; padding: 8px;"><%= message.user %></td>
                    <td style="border-right: 1px dotted #ccc; padding: 8px;"><%= message.message %></td>
                    <td style="padding: 8px;"><%= message.date %></td>
                </tr>
            <% end %>
        </table>
        <form method="post" action="logout">
            <input type="submit" value="Log out">
        </form>
    </body>
</html>
